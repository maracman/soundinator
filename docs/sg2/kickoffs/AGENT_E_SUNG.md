You are Agent E on the Sound Generator 2.0 project (repo = current directory).
Lane: the SUNG family end-to-end (soprano, mezzo-soprano, tenor, bass —
§9 decision 12; basso profondo and boy soprano are DERIVED presets after the
four sections pass gates). Read docs/sg2/AGENT_OPERATING_PROTOCOL.md FIRST
and follow its read order, then docs/sg2/SUNG_PREFLIGHT.md (its V0 rules are
your family's constitution) and docs/sg2/RESEARCH_SUNG_REALISM.md. Fresh
context: tenor pass-02 state is at HEAD; identity singers are approved
(tenor=m3, bass=m8, mezzo=f5; soprano primary TBD by your evidence procedure
from the repaired voice-soprano/ corpus).

Priorities, in order (from the owner-commissioned integration audit):
1. ROUTE THROUGH THE CANONICAL STACK: your pipeline hand-writes its gate
   tables and never calls evaluate_construction/evaluate_tripwires —
   wire both in (coordinate with Agent D, who is fixing the d.12
   voice-class aliases that would otherwise silently skip your gates).
   Mechanical §2.3 consumption via the canonical path, not manual.
2. §2.4c adoption: sung_fit.py initialises from neutral zeros — the exact
   banned anti-pattern (F2). Re-initialise every voice from the legacy
   `vocal` craft prior per the §2.4c table (envelope/vibrato/breath
   idioms), your fitted per-vowel bodies on top; ship-mode renders (F1).
3. Select the soprano primary (evidence procedure, rationale recorded);
   build its reference set; begin its fit alongside tenor refinement.
4. Consonant onsets: consonants-spoken/ (LibriSpeech + alignments) is
   landed; implement the sung-adaptation step (annex S31-S33) and the
   consonant onset features/generator spec to Agents D/A — zero weight
   until the controllability audit passes (F11).
5. Request the sung family-firewall assertion from D (F5/F12).
6. §2.5c differential fits from same-singer repeats when each identity
   stabilises; distributional gate before any freeze.
Begin now. Merge green work to codex/sg2-l4-l5-engine every pass.
