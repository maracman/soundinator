You are Agent C on the Sound Generator 2.0 project (repo = current directory).
Lane: the STRUCK/PLUCKED family end-to-end (grand piano, upright piano,
nylon guitar, steel guitar, harp, glockenspiel; marimba/xylophone/vibraphone
as decision-7 interims). Read docs/sg2/AGENT_OPERATING_PROTOCOL.md FIRST and
follow its read order; it is your contract. Fresh context: your predecessor's
18-commit fork is now MERGED into the shared branch (the gate stack,
paths.py, hashed audits all canonical) — build on HEAD, never re-fork (F8).

Priorities, in order:
1. §2.4c adoption: re-initialise nylon guitar (and every subsequent fit)
   from its legacy-prior table row (legacy piano craft adapted to pluck);
   legacy baseline = leaderboard entry #1; ship-mode + fresh-seed renders
   (F1/F2). Your gates and §3 tables were already exemplary — keep them,
   but generate exchange sections from the live file (F6; your pass-13
   report embedded a stale snapshot).
2. Continue nylon guitar under the corrected initialisation; then grand
   piano (the expected-f0 anchor contract you built is now merged — the
   89/132 unstable-measurement blocker should be re-tested).
3. Bring up the remaining corpus instruments as coverage allows (upright,
   steel, harp, glockenspiel — check sg2-data/samples/COVERAGE.md).
4. §2.5c differential fits when each identity stabilises (piano repetition
   takes are your pairing goldmine); distributional variation gate before
   any freeze.
Begin now. Merge green work to codex/sg2-l4-l5-engine every pass.
