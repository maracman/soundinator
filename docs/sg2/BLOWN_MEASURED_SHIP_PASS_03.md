# Blown measured-SHIP pass 03 — 2026-07-17

## Scope and outcome

This pass lands the missing engine consumer for the five blown
`humanRanges`, consumes Agent E's A-VOICE-04/T-061 and A-VOICE-03 handoffs,
and reruns the candidate distribution gates with six fresh SHIP seeds. No
identity parameter, measured range, factory preset, or leaderboard row was
widened or replaced.

All five distribution gates still fail, but sterility is materially reduced:
passing checks rise from 11 to 45 in aggregate and `too-little` checks fall
from 204 to 166. The new sparse `too-much` cells are retained as evidence;
there is no post-hoc multiplier. Every session remains `limiting-factor`
because construction, strict tripwires, and two-sided variation do not pass on
one renderer contract.

Owner L16 is acknowledged as a future engine handoff only. The anticipated
home is a neutral, measurement-pinned per-partial/per-band envelope-deviation
layer with velocity coupling. No schema or consumer was guessed before the
struck/analysis lanes deliver fixed-Hz versus harmonic-rank assignments and
the fitted gate curve.

## T-063 measured episode law

Engine commit `b5f91b7` consumes physical observables from each qualified
range rather than the current bow-centric candidate labels. Human controls
whether a note enters a measured performance episode. An active episode uses
the emitted `drawHalfRange` verbatim, bounded to the emitted 5–95% support,
and applies it as a delta around the strongest-prior craft centre. Human 0
draws nothing and FIT mode is unchanged. A separate seeded stream prevents
these timbre episodes from moving the melody generated by the same seed.

The consumer currently reaches excitation position, breath level in dB,
attack-noise level, blown onset deviation/settle, and the measured vibrato
trajectory. Unqualified outcome-only observations such as noise centroid and
lead remain unconsumed pending Agent D's excitation-specific registry and
responder audit. This is the evidenced reason many cells remain `too-little`.

## Six-seed distribution rerun

Run ID: `blown-measured-ship-r4`. The old column is the immediately preceding
six-seed candidate gate; the new column uses the same best params and reference
sets with only the measured episode consumer changed.

| Instrument | Old pass / little / much | New pass / little / much | Result |
|---|---:|---:|---|
| Flute | 4 / 54 / 3 | 10 / 47 / 4 | FAIL, less sterile |
| Clarinet | 2 / 30 / 1 | 6 / 26 / 1 | FAIL, less sterile |
| Alto sax | 2 / 59 / 0 | 11 / 48 / 2 | FAIL, less sterile |
| Trumpet | 0 / 9 / 0 | 2 / 7 / 0 | FAIL, less sterile |
| French horn | 3 / 52 / 0 | 16 / 38 / 1 | FAIL, less sterile |

The generated gate payloads and per-feature cells live at
`sg2-data/runs/<instrument>/blown-measured-ship-r4/ship-mode/candidate/variation-gate.json`.
Each run summary and its state backstop carry the renderer source hash.

## §3 gate table, including legacy rows

The new engine branches are exact-neutral in FIT/Human-0, so construction and
strict tripwire results are unchanged from the sealed best/legacy evaluations.
The candidate distribution row is the new r4 evidence. Legacy distribution
counts below are the preceding six-seed baseline anchor; they are shown for
the mandatory leaderboard-entry-1 comparison and are not claimed as a fresh
consumer pass.

| Preset row | Construction fails | Tripwire cells fail | Distribution pass / little / much | Gate |
|---|---:|---:|---:|---|
| Flute candidate r4 | 4 | 24 | 10 / 47 / 4 | FAIL |
| Flute legacy baseline | 4 | 24 | 4 / 55 / 2 | FAIL |
| Clarinet candidate r4 | 5 | 21 | 6 / 26 / 1 | FAIL |
| Clarinet legacy baseline | 5 | 22 | 3 / 30 / 0 | FAIL |
| Alto sax candidate r4 | 5 | 23 | 11 / 48 / 2 | FAIL |
| Alto sax legacy baseline | 5 | 23 | 1 / 60 / 0 | FAIL |
| Trumpet candidate r4 | 4 | 24 | 2 / 7 / 0 | FAIL |
| Trumpet legacy baseline | 4 | 24 | 0 / 9 / 0 | FAIL |
| French horn candidate r4 | 8 | 27 | 16 / 38 / 1 | FAIL |
| French horn legacy baseline | 8 | 27 | 1 / 54 / 0 | FAIL |

No leaderboard best changes. The five
`sg2-data/state/<instrument>/leaderboard.json` backstops remain byte-current
with their durable leaderboards; r4 gate summaries are copied beside them as
`ship-variation-r4.json`.

## Hierarchy-ordered construction/tripwire triage

The live measured order places `band_balance_db` first, followed by
`log_mel_db`, `partials_db`, `attack_ms`, then decay/envelope and downstream
noise/onset/Human features. Triage therefore remains:

1. All five: clear band balance first; every instrument still fails its
   construction band-balance assertion and 5–6 strict cells.
2. All five: clear the six mel and six partial-table cells before interpreting
   downstream noise/onset spread as an identity problem.
3. Flute/trumpet/horn: attack/envelope cells remain the next temporal layer;
   clarinet and alto sax retain 4–5 attack cells.
4. Only after those masks clear: fit the remaining breath/turbulence/body-air,
   onset-spectrum, articulation, and distribution consumers.

The complete generated ordering, failing construction IDs, and per-bar cell
counts are in `sg2-data/state/agent-a-pass03-hierarchy-triage.json`. Flute's
pinned split-half body peak disagreement is retained for analysis review; it
is not broadened in the engine.

## Agent E consumers

### A-VOICE-04 / T-061

`_renderBlowFloor` now amplitude-modulates its existing body-routed airflow
source at the exact scheduled source pitch, including onset approach, wander,
and vibrato. It adds no second noise source. Offline assertions prove:

- omitted sync and explicit zero are PCM-identical;
- sync 0.8 produces a tracked-f0 noise-envelope line at least 6 dB above
  adjacent bins and the same-seed zero render;
- an octave pitch move doubles the modulation peak within 2%;
- body on/bypass changes breath colour without moving pulse frequency.

Agent D still owns `pitch_sync_breath_db` and the fresh responder audit before
the feature can receive weight or Agent E can fit a nonzero value.

### A-VOICE-03

The neutral consonant schema and consumer are present: licensed/QC provenance
gate, plosive burst plus VOT, pitched nasal murmur, fricative noise, and an
F1/F2 onset colour that settles exactly to the fitted vowel body. Consonant
strength uses the same seeded articulation latent, so stronger articulation
raises consonant energy while reducing breath lead and scoop.

The generated audit at
`sg2-data/campaigns/sung-consonants/CONSONANT_ENGINE_AUDIT.json` now has
`generatorLanded=true` and `consumingAssertionLanded=true`. Its status is
correctly `blocked-zero-weight`: the adapted spoken-corpus features remain
watch-only until Agent D demonstrates responsiveness.

## Controllability and prior contract

SHIP-only episodes do not change the sealed FIT audits. Every audit remains
clean with zero uncontrolled weighted features.

| Instrument | Objective/audit hash | Legacy lookup row | Resolved parameter hash |
|---|---|---|---|
| Flute | `d30648e15e6cc119` | `flute ← legacy flute` | `704ba5bbca24f99a…` |
| Clarinet | `9575c25ca4bd5e68` | `clarinet ← legacy clarinet` | `014b7999e44481cc…` |
| Alto sax | `79802ec99ee04c6d` | `alto-sax ← legacy clarinet` | `dc1f40565e4ddd6a…` |
| Trumpet | `c3540929aa8dfabb` | `trumpet ← legacy trumpet` | `3d1ab496eac94d71…` |
| French horn | `a5e15cfcf130f152` | `french-horn ← legacy trombone` | `aed8af1b92463a80…` |

## Criteria drift and exchange state

The r4 work is SHIP-only and therefore emits no optimiser acceptance event;
no drift edge is fabricated from seeded distribution renders. The matrix was
rebuilt from its accepted-step rows and is byte-equivalent to the live state.
At the pass seal it contains 91 accepted steps, 65 directed transitions, 17
significant measured edges, and 82 symmetric couplings. Blown retains 42
accepted steps and 37 directed transitions, the largest family contribution.
Concurrent lanes therefore advanced the shared matrix from the 11-edge
pass-start snapshot to 17 without inventing an event for this SHIP-only pass.

Exchange status is generated from the live file at
`sg2-data/state/agent-a-pass03-exchange-statuses.json`. T-057, T-061, T-062,
and new cross-family method T-063 are reconciled against commit `b5f91b7`.

## Exit state and pass deliverables

Exit state for all five instruments: `limiting-factor`. The measured episode
consumer improves the distribution evidence but cannot close unqualified
observables or masked upstream identity cells. The next admissible blown work
is the hierarchy order above plus Agent D's excitation-specific Human adapter;
hand-widening is explicitly rejected.

Pass artifacts include the five r4 run summaries, five state backstops,
generated hierarchy triage, generated exchange statuses, unchanged/current
leaderboards, a fresh-seed `sg2-data/listen.html` rebuild, and the full green
verification suite. No mandate is silently pending: L16 is spec-waiting,
T-061 awaits analysis consumption, and consonant feature activation awaits the
zero-weight responsiveness audit.
